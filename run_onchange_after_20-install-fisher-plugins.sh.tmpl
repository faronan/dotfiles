#!/bin/bash
# fish_plugins hash: {{ include "dot_config/fish/fish_plugins" | sha256sum }}
set -euo pipefail
{{ if eq .chezmoi.os "darwin" -}}
echo "Installing Fish plugins..."
fish -c "fisher update" || {
    echo "⚠️  Fishプラグインのインストールに失敗しました"
    echo "後で再試行: fish -c 'fisher update'"
}
{{ end -}}
