// ===========================================
// Default Layout with zjstatus
// ===========================================
// zjstatus: カスタマイズ可能なステータスバー
// https://github.com/dj95/zjstatus

layout {
    default_tab_template {
        // zjstatus ステータスバー (上部)
        pane size=1 borderless=true {
            plugin location="file:~/.config/zellij/plugins/zjstatus.wasm" {
                // --- Format ---
                format_left  "{mode} {tabs}"
                format_right "{session} {datetime}"
                format_space "#[bg=#1e1e2e] "

                // --- Mode Indicator ---
                // Catppuccin Mocha colors
                mode_normal   "#[bg=#a6e3a1,fg=#1e1e2e,bold] NORMAL #[bg=#1e1e2e] "
                mode_locked   "#[bg=#f38ba8,fg=#1e1e2e,bold] LOCKED #[bg=#1e1e2e] "
                mode_resize   "#[bg=#cba6f7,fg=#1e1e2e,bold] RESIZE #[bg=#1e1e2e] "
                mode_scroll   "#[bg=#94e2d5,fg=#1e1e2e,bold] SCROLL #[bg=#1e1e2e] "
                mode_session  "#[bg=#fab387,fg=#1e1e2e,bold] SESSION #[bg=#1e1e2e] "
                mode_rename_tab "#[bg=#f9e2af,fg=#1e1e2e,bold] RENAME #[bg=#1e1e2e] "
                mode_default_to_mode "normal"

                // --- Tabs ---
                tab_normal              "#[fg=#6c7086] {index}:{name} "
                tab_normal_fullscreen   "#[fg=#6c7086] {index}:{name} [] "
                tab_normal_sync         "#[fg=#6c7086] {index}:{name} <> "
                tab_active              "#[fg=#89b4fa,bold] {index}:{name} "
                tab_active_fullscreen   "#[fg=#89b4fa,bold] {index}:{name} [] "
                tab_active_sync         "#[fg=#89b4fa,bold] {index}:{name} <> "

                // --- Session & DateTime ---
                datetime          "#[fg=#6c7086] {format} "
                datetime_format   "%H:%M"
                datetime_timezone "Asia/Tokyo"
            }
        }
        children
    }
}
